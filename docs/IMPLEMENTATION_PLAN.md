# Implementation Plan

## Backlog — Medium

### Streaming UI (Audio + Video)
- „Ähnliche Titel" vorschlagen (Artist/Genre/Album bzw. TMDB) (zurückstellen)

### Video-spezifisch
- Multi-Language-Linking — Phase 2: Fuzzy-Name-Matching (Phase 1b + manuelles YAML-Mapping via `language_group` sind abgeschlossen)
- Englische Serien: Metadaten + Titel in Englisch laden
- „Intro überspringen" (TMDB-Daten oder manuelle Markierung)
- Untertitelfiles + TMDB-Integration bei Umbenennungen
- `hometools_overrides.yaml` Erweiterung: weitere Override-Felder bei Bedarf (z.B. `tmdb_id`, `imdb_id`)

### Infrastruktur
- Tools-Code restrukturieren + umfassende Tests (Edge Cases, Dummy-Dateien)
- Optionales HTTPS
- iOS Background Video Playback → [plans/background_video_playback.md](plans/background_video_playback.md)

## Backlog — Low / Experimental

- **DJ-Extension** — Mixing, Stems (Gesang/Instrumental/Beat), BPM-Analyse, Auto-DJ-Modus
- **„MTV"-Modus** — Musikvideos + visuelle Begleitung zu Musik
- **„Sleep Mode"** — nur Audio aus Serien, kein Bildschirm
- **Photo-Management-Server**
- **HTTP-Obscurification** — Port-Knock statt HTTPS für Privat-Server
- **Pro-Nutzer Ordnerstruktur** (N8N-Integration)
- **Lennyface-Board**

## Mobile Features (postponed)

- **Phase 3: Native iOS Apps** — Hybrid WebView Wrapper für Video + Audio → [plans/native_app_plan.md](plans/native_app_plan.md)

## Design Discussions

### Smart-Playlist-Kaskaden (Phase 2)

**Status:** offen.

In Phase 1 (implementiert 2026-05-18) werden `in_playlist`-Regeln nur gegen *nicht-smarte* Playlists aufgelöst — Verweise auf andere Smart Playlists liefern garantiert `false` und werden im Index-Bau übersprungen.  Damit sind Zyklen ausgeschlossen, ohne dass eine Topologie-Analyse nötig ist.

**Phase-2-Vorschlag** (sobald praktisch benötigt):

1. Vor der Auswertung einen gerichteten Graphen `pl_id → set(referenzierte pl_ids)` aus allen `in_playlist`-Regeln aufbauen.
2. Per DFS auf Zyklen prüfen.  Bei Zyklus: betroffene Smart Playlists markieren, im UI mit einem Warn-Badge versehen und in der Auswertung wie leer behandeln.
3. Bei zyklenfreiem DAG topologisch sortieren und Smart Playlists in dieser Reihenfolge auswerten — vorgelagerte Ergebnisse stehen damit nachgelagerten als „virtuelle Playlist-Mitgliedschaft" zur Verfügung.
4. Tiefen-Limit (z.B. max. 5) als Safety-Net gegen Performance-Spitzen.
5. Cache der Auswertungsergebnisse pro Refresh-Zyklus (Memoization), damit eine Smart Playlist nicht mehrfach evaluiert wird, wenn sie von mehreren anderen referenziert wird.

**Trade-offs:**  Komplexität + UX-Kosten (Fehlermeldung „Zyklus erkannt") gegen die seltene Nützlichkeit (Power-User-Feature).  Bis ein User-Wunsch existiert, bleibt Phase 1 als bewusst einfache Lösung.

### `added_at`-Feld auf MediaItem (Phase 2)

**Status:** offen.

Smart Playlists nutzen aktuell `MediaItem.mtime` als Proxy für „Datum hinzugefügt".  Problem: Tag-Edits oder NAS-Resync setzen `mtime` zurück, sodass „Zuletzt hinzugefügt" Treffer „verlieren" kann.

**Vorschlag:** Neues Feld `first_seen_at: float` auf `MediaItem`, das beim ersten Build im Index-Cache vermerkt und nie überschrieben wird.  `catalog.py` (Audio + Video) liest den Wert aus dem vorigen Snapshot vor dem Persistieren des neuen.  Bei vollständigem Cache-Wipe fällt der Wert auf `mtime` zurück.  Smart-Playlist-Evaluator bevorzugt `first_seen_at`, fällt auf `mtime` zurück.

## Done

- **Global Search zeigt Ordner-/Serien-Treffer zuerst, Indexing-Toast antippbar, Faststart-Prewarm im Hintergrund** (2026-05-29) — Drei zusammenhngende UI-/Performance-Fixes:
  **(1) Global Search:** `globalSearch()` in `_player_js.py` baut zustzlich eine Folder-Match-Liste aus allen `relative_path`-Prefixes, deren Segment (oder `cleanFolderName()`-Variante) den Such-Begriff enthlt. Bei Video sind das Serien-Titel (Top-Level-Ordner). Folder-Matches werden **vor** den einzelnen Tracks/Episoden gerendert (`.search-folder-item`, Folder-Icon `IC_FOLDER_PLAY`, "Ordner"-Label, Klick  `navigateToSearchFolder(path)`  verlsst Search-Modus, `currentPath = path`, `showFolderView()`). Items pro Folder werden gezhlt, Sortierung: flache Tiefe zuerst, dann grsserer Count, dann alphabetisch. Header zeigt `"N Ordner + M Videos"` statt nur Track-Count. Hidden-Threshold-Filter wird respektiert. Neue CSS-Regeln `.search-folder-item`, `.search-folder-count` in `_css.py`.
  **(2) Indexing-Toast antippbar:** `.ht-indexing-toast` war auf schmalen Viewports ber dem Header-Suchfeld positioniert und verdeckte es. Fix in `_css.py`: Media-Query `(max-width: 600px)` verschiebt den Toast nach `bottom: calc(env(safe-area-inset-bottom) + 84px); right: 0.5rem;` (direkt ber der Player-Bar). Zustzlich `pointer-events: auto; cursor: pointer; user-select: none`. JS in `_player_js.py`: neuer `_indexToastDismissed`-Flag, Click-Handler im DOM-Constructor setzt das Flag und versteckt den Toast; `showIndexingToast()` early-return solange Flag steht; `hideIndexingToast()` (= Index fertig) reset Flag, sodass die nchste Indexing-Runde den Toast wieder anzeigt.
  **(3) Faststart-Prewarm im Background-Thumbnail-Worker:** Wurzelursache fr langsame Erst-Wiedergabe grosser MP4-Episoden (z.B. Avatar S01E01) war der **synchron** im Request-Handler laufende `ensure_faststart_cache()`-Call fr MP4-Dateien mit `moov`-am-Ende: `ffmpeg -c copy -movflags +faststart` musste die gesamte Datei vom NAS lesen und neu schreiben (Sekunden bis Minuten). Lsung: neuer `_prewarm_faststart_if_needed()` in `thumbnailer.py`, der nach jeder erfolgreichen Video-Thumbnail-Generierung gleichzeitig den Faststart-Cache pre-warmt. Bedingungen: Datei ist Browser-nativ (`!needs_remux`), `!has_faststart`, Cache fehlt/stale, Quelldatei  8 MiB. Best-Effort, exception-safe. Damit ist beim ersten Play-Klick der Cache bereits warm, Wiedergabe startet instant via `FileResponse` mit Range-Support. Wirkt sich auch auf `stream-prewarm`/`make video-prewarm` aus (gleicher Worker).
  Alle 1268 Tests grn, Ruff sauber, Feature-Parity 51/51. Doku in `architecture.md` (drei neue Sektionen: Background-Prewarm, Global Search Folder-Matches, Indexing-Toast Dismiss).

- **Docker-Deployment (Audio + Video, optional Channel)** (2026-05-24) — Reproduzierbares Multi-Stage-Image (`python:3.12-slim`, ffmpeg + tini, Non-Root-User `hometools` mit `PUID`/`PGID`-Build-Args fr Synology-Kompatibilitt). `Dockerfile` mit Builder-Stage (venv + `pip install`) und schlanker Runtime-Stage. `docker-compose.yml` startet pro Service einen eigenen Container vom gleichen Image: `audio` (Port 8010, `serve-audio`), `video` (Port 8011, `serve-video`), optional `channel` (Port 8012, auskommentiert), zustzlich auskommentierter `all-in-one`-Service. Gemeinsame Named-Volumes `hometools-cache` (`/data/cache`) und `hometools-audit` (`/data/audit`); Library-Mounts standardmig `:ro`. Healthcheck pro Service auf `/health` via `HC_PORT`-Env. `docker/.env.example` mit `PUID`, `PGID`, `AUDIO_LIBRARY_PATH`, `VIDEO_LIBRARY_PATH`, Port-Overrides. `.dockerignore` schliet Tests, Caches, Audit-Verzeichnis, `_*`-Dev-Artefakte und Secrets aus. Image-Env fixiert `HOMETOOLS_STREAM_HOST=0.0.0.0`, `HOMETOOLS_CACHE_DIR=/data/cache`, `HOMETOOLS_AUDIT_DIR=/data/audit`. Neue Doku `docs/docker.md` mit Quickstart, Synology-Schritt-fr-Schritt, Schreibrechte-Hinweis, Cast-Networking-Hinweis (`network_mode: host` bei VLAN-Trennung), Logs, Updates, Troubleshooting. README-Quickstart-Block ergnzt. `architecture.md`-Sektion „Docker-Deployment" hinzugefgt (Service-Tabelle, Konfigurations-Mapping, 8 Designregeln).

- **Cast-/AirPlay-Button im Video-Overlay** (2026-05-24) — Neuer `#video-cast-btn` im `video-overlay-header` (zwischen Titel und Fullscreen-Button), Icon `SVG_CAST` (klassisches TV-mit-WLAN-Bogen-Glyph). Nutzt ausschlielich Standard-Browser-APIs (kein Google-Cast-SDK, keine App-ID, kein Server-Code). **Chromium/Android-Chrome/Desktop-Chrome/Edge:** `player.remote.watchAvailability()` blendet den Button automatisch ein, sobald ein Cast-Ziel im Netzwerk gefunden wird; Klick ruft `player.remote.prompt()`; `connect`/`disconnect`-Events setzen `.active`-Klasse (Akzentfarbe). **iOS Safari:** Fallback ber `webkitplaybacktargetavailabilitychanged` + `webkitShowPlaybackTargetPicker()` (AirPlay-Picker). **Firefox/Safari-macOS/WebViews ohne Support:** Button bleibt `hidden` — keine Regression. Video-only: der Button existiert nicht im Audio-Player, da nur `<video>` die Remote-Playback-API hat. Neue CSS-Klasse `.video-cast-btn` (Mirrors `.video-fs-btn`-Shape, Akzentfarbe bei `.active`, `[hidden]`-Display-None). `SVG_CAST` zu Rule 13 (`copilot-instructions.md`) und `architecture.md` ergnzt.

- **Video-Server UI-Anpassungen: Tools-Row ohne Playlists, Eck-Sprachflagge** (2026-05-24) — Zwei video-spezifische UI-Anpassungen ber `_isVideo = (ITEM_NOUN === 'video')`-Laufzeit-Flag im gemeinsamen `_player_js.py`, kein duplizierter Code in `streaming/video/`. **(1) Tools-Row:** Karten „Neue Playlist…", „Intelligente Playlist…" und „Titel" werden auf dem Video-Root-Screen nicht gerendert (Playlists/Smart Playlists ohne Mehrwert ohne Queue-Mechanik, „Titel" redundant zur Ordneransicht). Audio-Server unverndert. Guard: `if (isRoot && PLAYLISTS_ENABLED && !_isVideo)`. **(2) Eck-Sprachflagge:** Video-Folder-Cards zeigen **immer** eine einzelne Sprachflagge in fester oberer rechter Ecke — auch bei mono-lingualen Ordnern ohne erkannte Sprache (Fallback `DEFAULT_LANG`). Neue CSS-Klasse `.folder-lang-corner` (absolute Positionierung, halbtransparenter dunkler Background fr Lesbarkeit auf hellen Thumbnails). Mono-lingual: Flagge = `f.languages[0] || DEFAULT_LANG` mit optionaler Sub-Sprache; der bisherige inline `langBadges`-Span neben dem Folder-Namen wird unterdrckt (keine Doppelanzeige). Multi-Variant: keine Eck-Flagge — die Variant-Flag-Buttons im `folder-count`-Bereich bernehmen die Anzeige. Audio-Server unverndert (kleine Inline-Badges bleiben nur bei tatschlich erkannten Sprachen).

- **iOS Auto-PiP Background-Playback-Fix** (2026-05-24) — Regression-Fix fr iOS 17+: Der `visibilitychange`-Handler im Video-Player rief `player.pause()` beim App-Hintergrund auf, was die Safari-`autopictureinpicture`-Transition abbrach. Fix: PiP-Status vor dem Pause prfen (`document.pictureInPictureElement === player` bzw. `player.webkitPresentationMode === 'picture-in-picture'`), und `player.pause()` nur auf Nicht-iOS und nur wenn nicht bereits in PiP. Background-Audio-Mirror wird ebenfalls nur aktiviert, wenn PiP nicht bernommen hat (sonst Doppel-Ton parallel zum PiP-Video). Desktop-Verhalten unverndert (pause + bg-audio-mirror verhindern Doppel-Ton bei Tab-Wechsel). `architecture.md`-Sektion „iOS-Background-Playback (Auto-PiP)" hinzugefgt.

- **asyncio-Disconnect-Log-Noise unterdrckt** (2026-05-24) — `asyncio`-Logger in `logging_config.setup_logging()` auf `ERROR` gesetzt. Browser-Disconnects bei Range-Requests (User skippt Video, iOS backgroundet, Pause) produzierten `WARNING socket.send() raised exception.` pro Abbruch und vermllten den Server-Log. Echte asyncio-Errors bleiben sichtbar.

- **Intelligente Wiedergabelisten (Smart Playlists)** (2026-05-18) — iTunes-artige Smart Playlists für Audio + Video.  Neues Core-Modul `streaming/core/smart_playlists.py` mit Operator-Registry (`eq`, `gte`, `lte`, `between`, `in`, `contains`, `starts_with`, `matches` [Regex, ≤ 256 Zeichen, Compile-Cache], `within_days`/`before`/`after` für `added_at`, `any_of`/`all_of`/`none_of` für `in_playlist`), AND/OR-Match-Modi, optionalem `sort` + `limit`, `is_favorite`-Feld, `in_folder` (Prefix-abgeleitet).  Pure functions, exception-safe, `validate_smart_rules()` für Endpoint-Sanity.  `playlists.py` erweitert um optionales `smart`-Argument in `create_playlist()` und neue Funktion `update_smart_rules()`; `add_item`/`remove_item`/`move_item`/`reorder_item` sind No-Ops auf Smart Playlists (Read-only-Guards).  Zwei neue API-Endpoints pro Server: `POST /api/<media>/playlists/smart` (Create), `PUT /api/<media>/playlists/smart` (Update/Promote).  **Client-seitige Auswertung** (`_evaluateSmartPlaylist()` als JS-Mirror der Python-Implementierung): `_resolvePlaylistItems()` erkennt `pl.smart` automatisch und expandiert Regeln gegen `allItems`, sodass bestehende Render-/Play-Pfade unverändert funktionieren.  Refresh-Button (`.playlist-folder-refresh`) auf jeder Smart-Karte ruft `refreshSmartPlaylist()` für Re-Evaluation auf.  UI: Lightning-Bolt-Badge (`SVG_SMART_PLAYLIST` als `.smart-pl-badge`) in der rechten unteren Ecke des `IC_PLAYLIST`-Logos kennzeichnet Smart Playlists.  Neue „Intelligente Playlist…"-Karte (`#smart-playlist-new-card`) rechts neben „Neue Playlist…" in `.playlist-tools-row` öffnet einen JS-DOM-injizierten Editor-Modal (`#smart-editor-backdrop`, `.smart-editor-modal`) mit Name-Input, AND/OR-Radio, dynamisch typabhängigen Regelzeilen (Text/Number/Multi-Select für Playlists/Boolean-Select/zwei Inputs für `between`), „+ Regel"-Button und optionalem Limit-Feld.  `PLAYLISTS_SMART_PATH` als neue JS-Variable.  `added_at` ist Phase-1-mäßig ein Proxy für `MediaItem.mtime`; ein dediziertes `first_seen_at`-Feld ist als Design Discussion dokumentiert.  Kaskaden (Smart referenziert Smart) sind Phase 1 absichtlich gesperrt — Verweise auf Smart Playlists in `in_playlist`-Regeln werden beim Index-Bau übersprungen.  40 neue Tests (`test_smart_playlists.py`): Operatoren (10), AND/OR (2), `in_playlist` inkl. Best-of-Rock + Cycle-Schutz (4), Sort/Limit (2), Validierung (4), Storage-Roundtrip + Read-only-Guards (4), API Audio (5) + Video (1), JS-Injection (4).  Architektur-Doku aktualisiert; SVG-Konstante `SVG_SMART_PLAYLIST` zu Rule 13 ergänzt.  Alle 1268 Tests grün.

  *Follow-up (gleicher Tag):* `in_playlist`-Wertinput von `<select multiple>` auf eine Checkbox-Liste (`.smart-rule-pl-list` mit `.smart-rule-pl-opt`-Labels) umgestellt — das Multi-Select war auf Touch praktisch unbenutzbar und auf Desktop erforderte es Ctrl+Click.  Empty-State („Keine regulären Playlists vorhanden") wenn der User noch keine nicht-smarte Playlist hat. `_smartCollectFromDom()` liest jetzt `querySelectorAll('.smart-rule-pl-cb')`.

- **`hometools scan-library` — Bibliotheks-Struktur-Analyse** (2026-05-18) — Neuer CLI-Befehl `hometools scan-library [--media video|audio] [--library-dir PATH] [--json] [--fail-on-warning]`. Neues Modul `streaming/core/library_scan.py` scannt die Medienbibliothek rein dateisystembasiert (kein ffprobe, kein TMDB) auf drei häufige Organisationsprobleme. **Checks (Video):** `episode_naming` (warning) — Ordner mit ≥ 4 Videodateien, bei denen weniger als 50 % einen S##E##-Pattern enthalten → Hinweis auf `hometools generate-overrides`; `oversized_folder` (info) — Ordner mit > 30 direkten Videodateien ohne Unterordner-Struktur; `untagged_language` (info) — Top-Level-Ordner ohne Sprach-Tag im Namen UND ohne `language`/`language_group`-Override in `hometools_overrides.yaml`. **Checks (Audio):** `oversized_folder` (info, Schwelle > 100 Audiodateien). `ScanReport.to_dict()` für JSON-Output. `--fail-on-warning` gibt Exit-Code 1. Schwellen sind keyword-only Parameter für testbare Konfigurierbarkeit. Exception Safety: alle Public-Funktionen liefern leere `ScanReport`-Instanz bei Fehler. 36 neue Tests (`test_library_scan.py`). Alle 1228 Tests grün. Gleichzeitig Backlog bereinigt: „Geräteübergreifende Fortschritts-Synchronisation" als erledigt markiert (seit Tag 1 server-seitig via `progress.py`), „Multi-Language-Linking Phase 2 manuelles Mapping" als erledigt markiert (`language_group`-Feld in `hometools_overrides.yaml` seit April implementiert).

- **Tools-Row Reihenfolge: Neue Playlist | Titel | Downloaded | reload** (2026-05-18) — Reihenfolge der Karten in `.playlist-tools-row` auf der Root-Startseite angepasst. Vorher: Downloaded → Titel → Neue Playlist → reload. Jetzt: Neue Playlist → Titel → Downloaded → reload. Beide `PLAYLISTS_ENABLED`-Karten werden in einem `if`-Block zusammengefasst (kein Doppelung). Alle 1192 Tests grün.

- **Bugfix: Stern-Rating sofort in Liste sichtbar + 0-Sterne-Toggle** (2026-05-18) — Zwei zusammenhängende Rating-UX-Bugs behoben. **(1) Listensync:** Nach einem Rating-Setzen (Player-Bar oder Inline-Sterne) war die `.rating-bar` im Listenitem bis zum nächsten Reload veraltet. Ursache: `setRating` / `setInlineRating` aktualisierten nur `filteredItems[idx].rating` (in-place-Mutation) und die sichtbaren Sterne, aber nicht (a) `allItems[i]` (der re-render nach `applyFilter` las stale Daten) und nicht (b) die `.rating-bar`-DOM-Element im `<li>`. Fix: Neue Hilfsfunktion `_patchAllItemsRating(relativePath, rating)` aktualisiert `allItems` sofort via `Object.assign` (konsistent mit dem bestehenden `refresh-ratings`-Pattern). Neue Hilfsfunktion `_updateTrackRatingBar(idx, rating)` patcht `.rating-bar` (erstellt/entfernt bei 0) und Inline-Sterne im `<li>` ohne Full-Re-Render. Beide Helpers werden in `setRating`, `setInlineRating` und `undoRating` aufgerufen. **(2) 0-Sterne-Toggle:** Bisher war kein Weg vorgesehen, eine Bewertung auf 0 zurückzusetzen. Toggle-Logik ergänzt: Klick auf den bereits aktiven Stern (d.h. `clicked === current`) ruft `setRating(0)` / `setInlineRating(idx, 0)` auf, statt die gleiche Zahl erneut zu setzen. Das Edit-Modal hatte dieses Pattern bereits (`n === _editModalRating ? 0 : n`) — Player-Bar und Inline-Sterne sind jetzt konsistent. `renderPlayerRating` setzt zudem ein Tooltip-Hint am aktiven Stern: *"Bewertung entfernen (nochmals klicken)"*. Toast-Text bei 0 Sternen zeigt *"Bewertung entfernt"* statt *"0 Sterne vergeben"*. 8 neue Tests, alle 1192 Tests grün.

- **`hometools validate-overrides` CLI + Override-Linter** (2026-05-17) — Neuer Lint-Subcommand `hometools validate-overrides [--library-dir PATH] [--json] [--fail-on-warning]` scannt rekursiv alle `hometools_overrides.yaml` in einer Bibliothek und meldet typische Fehlerquellen. Modul: `streaming/core/overrides_validator.py` (read-only, exception-safe). **Checks** mit stabilen `code`-Strings für Suppression: `parse_error` (error), `unknown_language` (warning, gegen neue `KNOWN_LANGUAGE_CODES`-Set in `language.py`), `unknown_episode_key` (warning, Episode-Key ohne passende Datei → Tippfehler/Stale-Eintrag), `unknown_field` (warning, Tippfehler in Top-Level- oder Per-Episode-Keys wie `series_titel`), `empty_override` (info), `non_media_extension` (info), `lonely_language_group` (info, Cross-Folder). Rückgabetyp `ValidationReport` mit `to_dict()` für JSON-Output, `errors`/`warnings`/`has_errors` Properties. **CLI** mit menschlich lesbarem oder `--json`-Output (Logging in JSON-Modus auf CRITICAL gesetzt, damit Output parseable bleibt). Exit-Code: `0` clean, `1` bei Errors (oder mit `--fail-on-warning` auch bei Warnungen). `KNOWN_LANGUAGE_CODES` als neue Single Source of Truth in `language.py` (`frozenset` mit `is_known_language_code()`-Helper). 25 neue Tests (`test_overrides_validator.py`): KNOWN_LANGUAGE_CODES, empty Library, parse errors, alle Unknown-Language-Varianten, Episode-Key existence, unknown fields, info-checks, language-group collisions, Report-API, 4 CLI-Handler-Tests (Clean/Error/Fail-on-Warning/JSON). Alle 1185 Tests grün.
- **`EpisodeOverride`: Per-Episode `language` + `subtitle_language`** (2026-05-17) — Folgeschritt zum Folder-Level-Sprach-Override: `EpisodeOverride` um optionale Felder `language: str | None = None` und `subtitle_language: str | None = None` erweitert (`None` = nicht gesetzt). `_parse_overrides()` liest die Felder pro Episode und normalisiert (`strip().lower()`). `apply_overrides()`: Per-Episode-Werte gewinnen **explizit** über Folder-Fallback *und* Auto-Detection (konsistent mit `title`/`season`/`episode`). Anwendungsfall: gemischtsprachige Episoden in einem Ordner (z.B. eine englische Synchron-Folge in einer deutschen Serie). YAML-Beispiel im Modul-Header erweitert, `architecture.md` aktualisiert. 7 neue Tests (`TestEpisodeLanguageOverrides`): Load, Defaults, Lowercase-Normalisierung, Episode-wins-over-Folder, Episode-wins-over-Auto-Detection, Subtitle separat, Episode-Override ohne language-Felder lässt Folder-Fallback intakt. Alle 1160 Tests grün.
- **`hometools_overrides.yaml`: `language` + `subtitle_language` Override-Felder** (2026-05-17) — Erweiterung des Per-Folder-YAML-Override-Systems um zwei neue Top-Level-Felder, mit denen für Ordner **ohne** automatisch erkennbaren Sprach-Tag (z.B. `Malcolm Mittendrin` ohne `(german)`-Suffix) manuell ein Sprachcode hinterlegt werden kann: `language: "de"` und `subtitle_language: "en"` (ISO 639-1). `FolderOverrides` um beide Felder erweitert (`@dataclass(frozen=True, slots=True)`). `_parse_overrides()` liest die Felder, normalisiert (`strip().lower()`). `apply_overrides()` füllt die Felder eines `MediaItem` **nur dann** mit dem Override-Wert, wenn das Item bisher leer ist — der via `parse_language_tag()` / `parse_subtitle_hint()` automatisch erkannte Wert hat immer Vorrang (Auto-Detection ist die Wahrheit, YAML ist Lückenfüller). **Bugfix nebenbei:** Bisher hat `apply_overrides()` beim Override-Anwenden die Felder `genre`, `file_size`, `duration`, `bitrate` der `MediaItem`-Kopie nicht weitergegeben (alle wurden auf Default zurückgesetzt) — jetzt vollständig erhalten. Doku in `media_overrides.py`-Modul-Header (Beispiel-YAML) und `architecture.md` (Language-Tags-Sektion) aktualisiert. 8 neue Tests (`TestLanguageOverrides`): Load mit/ohne Felder, Lowercase-Normalisierung, Apply füllt leere Felder, Apply respektiert vorhandene Auto-Detection, Subtitle separat, Field-Preservation, Pass-Through ohne Override. Alle 1153 Tests grün.
- **Katalog-Refresh-Button in Tools-Row + Globale Tools im Panel** (2026-05-18) — Umstrukturierung des Katalog-Refresh-Workflows: (1) **Header-Button entfernt:** `<button id="refresh-btn">` wurde aus dem `<header>` entfernt. (2) **Tools-Row-Karte:** „Katalog neu laden" ist jetzt immer ganz rechts in der `playlist-tools-row` auf der Root-Startseite platziert als `<button class="tools-row-item refresh-catalog-card" id="refresh-catalog-card">` (gleiche Größe und Layout wie „Neue Playlist…"). Click-Handler via Event-Delegation auf `folderGrid`. `_getRefreshBtn()` (dynamische DOM-Abfrage) ersetzt die statische `_refreshBtn`-Variable. (3) **tools-pill-refresh entfernt:** Der versteckte Reload-Button innerhalb der Tools-Pill wurde entfernt (inkl. CSS und JS-Handler). (4) **„Ordnerdaten erneuern"-Section entfernt:** Die Buttongroup mit „Kopfleiste / Im Tools-Button / Aus" im Tools-Panel wurde entfernt. (5) **„Globale Tools"-Abschnitt im Tools-Panel:** Neuer Abschnitt am Ende des Panels mit (a) „Ordnerdaten aller Ordner erneuern"-Button (`#tools-global-refresh-btn`) → schließt Panel und ruft `refreshCatalog()`, (b) „Automatischer Katalog-Refresh"-Buttongroup (`#tool-auto-refresh`, Auto/Aus) → steuert ob `scheduleBackgroundRefresh()` beim Laden aufgerufen wird (`_toolState.autoRefresh`). (6) **`_applyHeaderUiState()` vereinfacht:** Entfernt Refresh-Position-Logik, bereinigt Legacy-Body-Klassen (`tool-refresh-off`, `tool-refresh-in-pill`). (7) **CSS-Bereinigung:** `.tools-pill-refresh`-Styles, `body.tool-refresh-off #refresh-btn`, `.refresh-btn`-Header-Styles entfernt. Neue Styles: `.refresh-catalog-card` (analog zu `playlist-new-card`), `.refresh-catalog-card.spinning .tools-row-icon svg { animation: spin }`, `.tools-global-refresh-btn`. Test `test_both_home_pages_include_refresh_button` aktualisiert (prüft jetzt `refresh-catalog-card`). Alle 1192 Tests grün.
- **Root-Tools-Row: Downloaded + Neue Playlist + Titel kompakt** (2026-05-17) — Die beiden großen `folder-card`-Quadrate „Downloaded" und „Neue Playlist…" wurden auf der Root-Startseite durch eine kompakte horizontale Leiste `.playlist-tools-row` (`grid-column: 1 / -1`) ersetzt, die zusätzlich eine dritte Aktion **„Titel"** enthält. „Titel" (`#all-titles-card`, `data-playlist-id="__alltitles__"`) öffnet eine Pseudo-Playlist-Ansicht, die alle Titel aus allen User-Playlists in einer **deduplizierten** Liste anzeigt (`_collectAllPlaylistRelPaths()` + `_resolveAllPlaylistItems()`). Die Karte ist nur sichtbar, wenn mindestens ein Titel in irgendeiner Playlist liegt. Die Pseudo-Playlist ist **read-only** — `reorderPlaylistItem()` und `movePlaylistItem()` sind für `__alltitles__` blockiert. CSS: `.playlist-tools-row` (flex, wrap, gap 8px), `.tools-row-item` (flex, padding 8/10, border-radius 8px, surface2-Background), `.tools-row-icon` (22×22), `.tools-row-label` (ellipsis), `.tools-row-count` (Pill mit `:empty { display: none }`). Klassen `.offline-folder-card`, `.playlist-new-card`, `.playlist-folder-card` bleiben für Selektor-Kompatibilität (Click-Handler, `test_feature_parity.py`) erhalten. Offline-Count zeigt jetzt nur noch die Zahl (`String(ready.length)`) statt „N downloads". `docs/architecture.md` aktualisiert. Alle 1145 Tests grün.
- **Waveform-Peak-Caching + Stereo-Visualisierung (Classic-Modus)** (2026-05-16) — **256 Segmente, Stereo-Split (L oben / R unten):** (1) **`streaming/core/waveform.py`:** `WAVEFORM_SEGMENTS = 256`. `extract_waveform_peaks()` dekodiert via ffmpeg stereo (`-ac 2`, 1 kHz f32le), deinterleaved L/R, berechnet Peak-Absolutwert pro Segment-Block, normalisiert L+R **gemeinsam** (relative Lautstärke bleibt erhalten). Rückgabetyp jetzt `tuple[list[float], list[float]] | None`. `ensure_waveform()` speichert `{"peaks_l": [...256], "peaks_r": [...256], "segments": 256}`. Backward-kompatibel: alte Caches mit nur `"peaks"` werden vom Client automatisch als Mono behandelt. `start_background_waveform_generation()` unveränderter Worker. (2) **`GET /api/audio/waveform`:** Endpunkt unverändert — serviert einfach das gecachte JSON, das jetzt `peaks_l`/`peaks_r` enthält. (3) **Classic-Mode JS:** `var waveformDataR = null` (neue Variable für rechten Kanal). `generateWaveform(url, relativePath)` liest `data.peaks_l` → `waveformData`, `data.peaks_r` → `waveformDataR` (Fallback auf `data.peaks` für Mono-Legacy-Cache). `drawWaveform(progress)` unterscheidet drei Zustände: **Stereo** (L-Kanal wächst von Mitte nach oben, R-Kanal von Mitte nach unten; gespielt = Akzentfarbe `alpha 0.72`, ungespielt = `#999` `alpha 0.28`; Layer 1 = dünne 1px Mittellinie); **Mono** (zentrierte Balken, semi-transparente weiß-Overlay wie bisher); **Leer** (5px Standard-Progressbar). (4) Test für Waveform-Server-Endpunkt auf 256 Segmente + `peaks_l`/`peaks_r`-Keys aktualisiert.
- **Duplikat-Ghost-Zeilen nach Löschung** (2026-05-16) — Nach dem Löschen eines Duplikats bleiben beide Tracks (gelöschter und überlebender) in der Liste sichtbar. `._deleted = true`-Marker statt `splice`: `_deleteDuplicateFile()` und `_deleteTrackFromList()` setzen diesen Marker. `applyFilter()` lässt `_deleted`-Items alle Filter passieren. `renderTracks()`: `_deleted`-Items werden aus `filteredItems` ausgeschlossen, danach als Ghost-Rows mit Klasse `.track-item--deleted` (Strikethrough, `opacity: 0.35`, `pointer-events: none`) an der ursprünglichen Position in `displayTracks` eingefügt (gleiches Muster wie `._movedTo`-Ghosts). Roter „Gelöscht"-Badge (`.deleted-badge`). Duplikat-Panel: gelöschte Items bekommen Klasse `.dupe-group-item--deleted` (kein Trash-Button, kein Click-to-Play). `markActive()` und Click-Handler schließen `.track-item--deleted` aus. Track-Counter zeigt `(N gelöscht)`-Suffix. CSS: `.track-item--deleted`, `.deleted-badge`, `.dupe-group-item--deleted`.
- **Classic-Mode Progress-Bar verbreitert (UI-Vorbereitung für Waveform)** (2026-05-16) — `.progress-track` in Classic-Modus: `height: 28px`, Box-Hintergrund + Border + Border-Radius als dezenter Container. Canvas-Element als visuelle Ebene, Range-Input transparent als Klick-Overlay. Implementiert gemeinsam mit Waveform-Caching (siehe oben).
- **Audit-Button in Tools-Panel-Header (oben rechts)** (2026-05-16) — „Änderungsverlauf"-Button aus dem Haupt-Header entfernt und als SVG-Icon-Button rechts im `.tools-panel-header`-Div (neben dem Panel-Titel) platziert. Neues `.tools-panel-header`-CSS (flexbox, space-between, margin-bottom). Der Audit-Button ist damit weniger prominent (selten genutzt) und trotzdem von jedem Tools-Panel-Aufruf direkt erreichbar.
- **Tools-Panel Überarbeitung: Audit-Button, Sektionsüberschriften, kein Downloaded-Pill** (2026-05-16) — Vier zusammenhängende UI-Bereinigungen: (1) **Änderungsverlauf-Button:** Aus dem Tools-Panel-Toggle-Bereich entfernt und als immer sichtbaren grauen Pill-Button direkt links neben „Tools & Einstellungen" in den Header verschoben. `body.tool-hide-audit`-CSS-Klasse + zugehörige JS-Logik (`_toolAuditBtn`, `_toolState.auditBtn`) entfernt. (2) **Downloaded-Pille entfernt:** Kein `<span id="downloaded-pill">` mehr im Normal-Mode-Header (nur noch bei `safe_mode` als „Safe Mode"-Indikator). Toggle-Option aus Tools-Panel und alle zugehörigen JS-Stellen (`_toolDownloadedPill`, `_toolState.downloadedPill`, `body.tool-hide-downloaded`) entfernt. `downloadedPill`-JS-Variable bleibt (null in Normal-Mode, wird via `if`-Guard abgesichert). (3) **Sektionsüberschriften im Tools-Panel:** Drei Abschnitte mit `.tools-section-heading`-Labels: „Titelbearbeitung" (Inline-Ratings, Dateien verschieben), „Bibliothek" (Downloads, Zur Playlist hinzufügen, Duplikate suchen), „Ansicht" (Ordnerdaten erneuern). CSS: klein, uppercase, `letter-spacing`, gedämpft. (4) **Buttongroup „Ordnerdaten erneuern" kein Wrap mehr:** Altes `tools-item` mit verschachteltem `div > buttongroup` durch `.tools-item--full` (block-Layout, full-width) ersetzt. Buttongroup auf `display: flex; width: 100%`, Buttons auf `flex: 1; text-align: center` — kein Zeilenumbruch mehr bei schmalem Panel. Tests: 2 Assertions auf `id="downloaded-pill"` aus `test_feature_parity.py` und `test_offline_downloads.py` entfernt (je mit Kommentar). Alle 1116 Tests grün.
- **URL-Routing erweitert: Sort/Filter/View-Mode/Tools-Panel in der URL** (2026-05-16) — Das bestehende `_router` (view/path/id/track/q) wurde um sechs zusätzliche Query-Parameter erweitert: `sort`, `fr` (Filter Rating), `ff` (Filter Favoriten), `fg` (Filter Genre), `fh` (Hidden ausblenden), `vm` (View-Mode grid/list), `panel=tools` (Tools-Panel-Modal). Neue Hilfsfunktionen `_collectUiState()`, `_applyUiStateFromUrl()`, `_applyPanelFromUrl()` in `_player_js.py`. `_applyUiStateFromUrl()` wird in `restore()` **vor** dem ersten Render aufgerufen — sodass z.B. `?fr=4` die Rating-Schwelle direkt setzt und die Liste schon gefiltert gerendert wird. URL gewinnt über `localStorage` und persistiert die übernommenen Werte zurück. Push-vs-Replace-Schlüssel bleibt `view|path|id|q`, d.h. Filter-/Sort-/View-Mode-/Panel-Änderungen erzeugen keine neuen History-Einträge (`replaceState`). Alle fünf UI-Handler (Search-Input, Sort-Field, Rating-/Fav-/Genre-/Hidden-Filter, View-Toggle, Tools-Panel open/close) rufen jetzt `_router.update()`. Audit-Panel ist eine eigene Route (`/audit`) und kein Modal — daher kein `panel=audit`. `docs/architecture.md` Tabelle und Regeln entsprechend aktualisiert. Alle 1116 Tests grün.
- **Audit-Panel SVG-Sterne + Rating-Undo-Fix** (2026-04-14) — Zwei Fixes: (1) **Visuelle Konsistenz:** Audit-Panel (`/audit`) verwendet jetzt identische SVG-Sterne (`IC_STAR_FILLED`/`IC_STAR_EMPTY`) wie Player-Bar, Inline-Ratings und Edit-Modal — statt Unicode `★☆` (Regel 13). History-Link-Emoji `` durch SVG-Clipboard-Icon ersetzt. Header-Emoji `` durch SVG-Notepad-Icon ersetzt. Neue CSS: `.audit-star`, `.audit-star.active`, `.star-value`. `.stars` auf `inline-flex` umgestellt. (2) **Format-bewusstes Rating-Undo:** `audio_audit_undo()` nutzt jetzt `set_rating_stars()` (format-bewusst: MP3/M4A/FLAC/OGG) statt `set_popm_rating()` (MP3-only). Zusätzlich `patch_items()` für sofortiges UI-Update. Bugfix: `old_raw=0` war in `audio_set_rating()` hardcodiert → jetzt `stars_to_popm_raw(old_stars)` — Undo stellt den tatsächlichen vorherigen Rating-Wert wieder her statt immer auf 0 zurückzusetzen. `architecture.md` aktualisiert.
- **Index-Cache: Snapshot-Staleness nach Serverneustart + Voller Katalog-Refresh** (2026-04-14) — Zwei zusammenhängende Fixes: (1) `IndexCache.get_cached()` setzt `_built_at = 0.0` beim Laden eines persistierten Snapshots, sodass `_is_fresh()` immer `False` zurückgibt und `ensure_background_refresh()` sofort einen Hintergrund-Rebuild startet — neue Dateien/Ordner (z.B. `#NEW`) erscheinen jetzt kurz nach Serverneustart statt erst nach TTL-Ablauf (bis zu 15 Minuten). (2) `refreshCatalog()` JS-Funktion ruft jetzt `POST /api/<media>/refresh` (voller Index-Rebuild + Cache-Invalidierung) auf statt `POST /api/<media>/refresh-ratings` (nur Ratings). Nach dem POST pollt `_refreshPoll()` das Tracks-API bis `refreshing: false`, ersetzt `allItems` komplett und zeigt Toast mit Diff-Info (N Titel geladen, +M neu / -M entfernt). Der Lazy-Per-Folder-Rating-Refresh (`refreshFolderRatings()`) bleibt separat für automatisches Ordner-Öffnen. `architecture.md` aktualisiert.
- **Non-blocking Video-Server-Start (Language-Groups)** (2026-04-14) — Root Cause: `load_language_groups()` in `create_app()` des Video-Servers rief `load_all_overrides()` → `rglob("*")` synchron auf der NAS-Bibliothek auf. Bei großen Bibliotheken (UNC-Pfad `\\Syn723\Serien\`) blockierte der Scan mehrere Minuten und verhinderte den Server-Start. Fix: Language-Group-Beladung in einen Daemon-Thread (`video-lang-groups`) verschoben. Der Thread prüft zuerst `check_library_accessible()` (3s-Timeout) und überspringt den Scan bei nicht erreichbarer Bibliothek. `_lang_state["json"]` wird dynamisch aktualisiert und bei jedem Request gelesen. Kein Blocking des Server-Starts mehr. `architecture.md` aktualisiert.
- **Multi-Language Phase 1b: Inline-Flaggen-Buttons + Untertitelsprache** (2026-04-13) — Erweiterung des Sprach-Tag-Systems um Untertitel-Erkennung und verbesserte Multi-Language-Navigation. **Backend:** Neue Funktionen `parse_subtitle_hint()` und `parse_language_full()` in `language.py` erkennen Untertitelsprachen aus zusammengesetzten Tags (z.B. `(engl, gersub)` → sub=de). Neues `MediaItem.subtitle_language`-Feld (ISO 639-1). `build_video_index()` befüllt beide Felder. Neue Config `get_default_language()` (`HOMETOOLS_DEFAULT_LANGUAGE`, Default `de`). **Frontend:** `detectSubLangFromName()` JS-Funktion für client-seitige Untertitel-Erkennung. `compositeFlagHtml(mainLang, subLang)` rendert zusammengesetzte Flaggen (große Hauptflagge + kleine Untertitel-Flagge in der Ecke). Multi-Language-Ordnerkarten zeigen jetzt inline `.lang-select-btn`-Buttons mit zusammengesetzter Flagge + Episodenanzahl statt einfachem Zähler. Klick auf Flaggen-Button → Direktnavigation in Variante. Klick auf Karte → Navigation in `DEFAULT_LANG`-Variante. `contentsAt()` aggregiert `subLang` pro Ordner. Varianten enthalten jetzt `subLang`-Feld. `DEFAULT_LANG` JS-Variable durch Config injiziert. **CSS:** `.composite-flag`, `.composite-flag-sub`, `.lang-select-btn` (hover + active styling). Snapshot-Version v7. `architecture.md` aktualisiert.
- **Sprach-Tags & Flaggen-Badges (Video)** (2026-04-11) — Automatische Erkennung von Sprach-Tags in Ordnernamen wie `(engl)`, `(eng)`, `(en)`, `(german)`, `(de)` etc. Neues Modul `streaming/core/language.py` mit `parse_language_tag()`, `strip_language_tag()`, `clean_folder_name()`. Neues `language: str`-Feld auf `MediaItem` (ISO 639-1). `build_video_index()` befüllt das Feld aus Ordnernamen. Sprach-Tags werden im UI entfernt und durch SVG-Flaggen-Badges ersetzt (10 Sprachen: DE, EN, FR, ES, IT, JA, KO, ZH, PT, RU). `cleanFolderName()` JS-Funktion strippt sowohl `#`-Prefix als auch Sprach-Tags — wird in `contentsAt()`, `leafName()` und `renderBreadcrumb()` verwendet. `contentsAt()` aggregiert Sprachen pro Ordner in ein `languages`-Array. CSS: `.lang-badge` (18×12px inline SVG-Flaggen). Video-Organizer (`series_rename_episodes`, `generate_overrides_yaml`) nutzt jetzt `clean_folder_name()` statt manueller Regex. Snapshot-Version v6. 44 neue Tests (28 language.py + 12 player_ui + 4 feature_parity). `architecture.md` aktualisiert.
- **Repeat-Modus (Off / Alle / Einzeltitel)** (2026-04-11) — Neuer Wiederholungs-Button im Player-Bar mit drei Modi: Off → Alle wiederholen → Einzeltitel wiederholen → Off. `SVG_REPEAT` wurde bereits als Konstante vorbereitet — jetzt mit Logik versehen. Neue JS-Ikonen-Variablen: `IC_REPEAT` (Standard), `IC_REPEAT_ONE` (mit „1"-Overlay). State: `repeatMode` (false / 'all' / 'one'), persistiert in `localStorage` (`ht-repeat-mode`). Neues Feature-Flag `enable_repeat` in `render_player_js()` / `render_media_page()`, JS-Variable `REPEAT_ENABLED`. Neue Funktionen: `cycleRepeat()`, `updateRepeatBtn()`. **Verhalten:** `repeat-one` → Track startet bei `ended` automatisch von vorn (`player.currentTime = 0`), kein Crossfade. `repeat-all` → `nextIndex()` wraps am Ende der Liste auf 0 zurück (bisheriges Default-Verhalten). `repeat-off` → `nextIndex()` gibt `-1` zurück am Ende der Liste → `playNextItem()` stoppt die Wiedergabe. Queue hat Vorrang (wird vor Repeat geprüft). Crossfade wird bei `repeat-one` unterdrückt (`repeatMode !== 'one'` Guard). CSS: `.repeat-btn.repeat-active`, `.repeat-btn.repeat-one`. Beide Server: `enable_repeat=True` (Audio + Video). 15 neue Tests (8 UI + 5 Logik + 2 Feature-Parity). `architecture.md` aktualisiert.
- **M4A Windows Xtra-Box Rating-Sync** (2026-04-11) — Root-Cause-Fix: Windows Explorer liest/schreibt M4A-Bewertungen über die Microsoft-proprietäre `Xtra`-Box (`moov/udta/Xtra → WM/SharedUserRating`, Werte 0/1/25/50/75/99), nicht über den iTunes-Freeform-Atom (`----:com.apple.iTunes:RATING`, 0–100). Beide Tags lebten unabhängig im selben File → Diskrepanz zwischen Windows-Anzeige und Player. **Fix:** `_read_m4a_rating()` bevorzugt jetzt die Xtra-Box (Windows-Wahrheit), fällt auf iTunes-Atom zurück. `_write_m4a_rating()` schreibt immer **beide** Tags synchron (iTunes via mutagen, Xtra via Raw-Binary-Manipulation). Xtra-Box-Format als Binary geparst/geschrieben, drei Write-Fälle: in-place Update (gleiche Größe), Attribut anhängen (Box-Größen anpassen), Box erstellen (neue Box in udta). Neue Funktionen: `_read_xtra_rating()`, `_write_xtra_rating()`, `_build_xtra_rating_attr()`, `_find_mp4_box()`, `_wm_rating_to_stars()`. Mappings: `_STARS_TO_WM`, `_WM_TO_STARS`. Snapshot-Version auf v5 gebumpt. 25 neue Tests: `TestWmRatingToStars` (8), `TestXtraRatingReadWrite` (8), `TestM4aDualTagSync` (2), `TestXtraRealFile` (3), `TestM4aRealFileRoundtrip` aktualisiert. `architecture.md` M4A-Sektion komplett überarbeitet.
- **Gezielter Refresh-Button** (2026-04-10, **abgelöst durch voller Katalog-Refresh 2026-04-14**) — Ursprünglich nutzte `refreshCatalog()` nur `POST /api/<media>/refresh-ratings` für Rating-Updates der angezeigten Items. Seit 2026-04-14 löst der Button stattdessen einen vollen Index-Rebuild via `POST /api/<media>/refresh` aus, der auch neue/gelöschte Dateien und Ordner erkennt.
- **M4A Rating Binary-Fallback + Roundtrip-Tests** (2026-04-10) — `_read_m4a_rating()` in `audio/metadata.py` versucht jetzt zuerst UTF-8-Text-Parsing (z.B. `b"80"` → 80) und fällt bei Fehlschlag auf binäre Integer-Interpretation zurück (ein Byte: Ordinalwert; mehrere Bytes: Big-Endian). Damit werden auch Ratings gelesen, die von Tools mit IMPLICIT-Datenformat geschrieben wurden. 7 neue Tests: `TestM4aRatingRoundtrip` (4 Tests mit echtem ffmpeg-generiertem M4A-File: Roundtrip 0–5 Sterne, fraktionale Rundung, Tag-Erhaltung, unrated=0.0) + `TestReadM4aRatingBinaryFallback` (3 Tests: single-byte, two-byte big-endian, empty-bytes). `architecture.md` M4A-Sektion aktualisiert.
- **Globale Suche (Root-View)** (2026-04-10) — Bibliotheksweite Echtzeit-Suche über alle Tracks direkt auf der Startseite. Das vorhandene aber leere `#folder-filter-bar`-Element wird auf der Root-Ansicht mit einem Suchfeld (`#global-search-input`) bestückt. 200ms Input-Debounce. Filtert `allItems` nach Titel/Artist/relative_path, respektiert `MIN_RATING_THRESHOLD`. Ergebnisse werden als Track-Liste mit Ordner-Pfad-Kontext (`.search-result-folder`) angezeigt. Klick auf ein Ergebnis navigiert in den zugehörigen Ordner und startet den Track. Escape/Leeren/Back-Button → zurück zur Ordner-Ansicht. `goBack()` erkennt `_globalSearchActive` und kehrt zum Root zurück. Rein client-seitig (kein neuer Backend-Endpoint). Shared Core (Audio + Video). Neue JS-Funktionen: `initGlobalSearch()`, `globalSearch()`, `renderSearchResults()`, `navigateToSearchResult()`, `exitGlobalSearch()`. Neue CSS: `#global-search-input`, `.search-result-folder`. 5 neue Tests, 2 Feature-Parity-Tests (`TestGlobalSearchParity`). `architecture.md` aktualisiert.
- **MIN_RATING-Schwellenwert-Fix: >= statt >** (2026-04-10) — Bugfix: Bei `HOMETOOLS_MIN_RATING=3` wurden Songs mit genau 3★ fälschlich ausgeblendet, weil der Filter `r > MIN_RATING_THRESHOLD` (strikt größer) statt `r >= MIN_RATING_THRESHOLD` (größer-gleich) verwendete. Semantik jetzt: `MIN_RATING=3` bedeutet „zeige alles ab 3★", d.h. 1-2★ = versteckt, 3★+ = sichtbar, unbewertet = immer sichtbar. Fix in beiden Zweigen von `applyFilter()` (Normal-Modus und Debug-Filter-Modus). Debug-Begründungstext von `≤` auf `<` korrigiert. 1 Test angepasst (`test_min_rating_filter_logic_in_apply_filter`). `architecture.md` aktualisiert.
- **Debug Filter Mode + Rating Refresh Log** (2026-04-10) — Zwei Features zur Transparenz der Rating-Filterung: 1) **Debug Filter Mode** (`HOMETOOLS_DEBUG_FILTER=true` in `.env`): Items, die durch `MIN_RATING` oder Quick-Filter ausgeblendet würden, werden stattdessen **ausgegraut mit Begründungstext** angezeigt (z.B. „Rating 2★ ≤ Schwelle 3"). Nicht klickbar/spielbar (`pointer-events: none, opacity: 0.35`). Track-Count im Header zeigt z.B. „3 tracks (+ 20 ausgeblendet)". Textsuche filtert weiterhin normal. Neue `get_debug_filter()` in `config.py`, Parameter-Pipeline durch `render_media_page()` → `render_player_js()` → JS `DEBUG_FILTER`. CSS: `.debug-filtered`, `.debug-reason`. Beide Server (Audio + Video). 2) **Rating Refresh Log**: Persistentes JSON-Log (`<cache_dir>/rating_refresh_log.json`) speichert pro Ordner wann die POPM-Ratings zuletzt vom Dateisystem gelesen wurden (Timestamp + Statistiken). Neuer `GET /api/audio/refresh-log` Endpoint. `POST /api/audio/refresh-ratings` Response erweitert um `last_refresh` und `folder`. UI zeigt Refresh-Info im Header-Element `#refresh-info` (z.B. „23 Ratings gelesen, 5 aktualisiert (14:30)"). **Bugfix:** `refreshFolderRatings()` JS ignorierte Responses mit `changed === 0` durch Bedingung `!data.changed` (falsy bei 0) — gefixt zu `!data.ratings`. 7 neue Tests, `architecture.md` aktualisiert.
- **Crossfade (Audio)** (2026-04-09) — Nahtlose Song-Übergänge: Ein zweites `<audio>`-Element (`_xfadeAudio`) wird lazy erstellt und spielt den nächsten Track ein, während der aktuelle Track ausgeblendet wird. Konfigurierbar via `HOMETOOLS_CROSSFADE_DURATION` (Env-Var, Default `0` = aus, Max `12s`). `get_crossfade_duration()` in `config.py`, als `crossfade_duration` Parameter durch `render_media_page()` → `render_player_js()` → `CROSSFADE_DURATION` JS-Variable injiziert. Nur Audio-Server (Video hat kein Crossfade). Trigger in `timeupdate`: verbleibende Zeit ≤ Dauer + Schutz gegen kurze Tracks (< Dauer+5s). 20-Schritt sinusoide Volume-Rampe (`0.5 - 0.5*cos(π*p)`). Abbruch bei Pause, manuellem Track-Wechsel. Queue-kompatibel (`_resolveNextForCrossfade` peeked). Rein client-seitig. 7 neue Tests (Config 2, JS-Injection 4, Audio-Server 1). `.env.example` aktualisiert.
- **POPM↔Sterne: WMP-Standard-Mapping** (2026-04-10) — Bugfix: Lineare Konvertierung (`raw/255*5`) durch Windows-Media-Player-Standard-Step-Mapping ersetzt. Neue Funktionen `popm_raw_to_stars()` und `stars_to_popm_raw()` in `audio/metadata.py`. Kanonische Werte: 0→0, 1→1★, 64→2★, 128→3★, 196→4★, 255→5★. Bereichs-basiertes Lesen: 0=unbewertet, 1–31=1★, 32–95=2★, 96–159=3★, 160–223=4★, 224–255=5★. Behebt: Bewertungen aus hometools wurden von externen Tools (foobar2000, MusicBee, Mp3tag, Windows Explorer) falsch gelesen und umgekehrt. Geänderte Stellen: `catalog.py` (build_audio_index), `server.py` (GET metadata, POST rating). Snapshot-Version in `index_cache.py` auf v2 gebumpt — alte Snapshots mit linear-gemappten Ratings werden verworfen und frisch vom Dateisystem gebaut. 23 neue Tests (TestPopmRawToStars, TestStarsToPopmRaw, TestPopmRoundtrip). `architecture.md` aktualisiert.
- **Lazy Per-Folder Rating Refresh** (2026-04-10) — On-demand POPM-Rating-Aktualisierung für angezeigte Tracks: Beim Öffnen eines Ordners werden nur die angezeigten Items (typisch 10–50) vom Dateisystem neu gelesen, statt den gesamten 5 000-Item-Katalog neu zu bauen. Neuer `POST /api/audio/refresh-ratings`-Endpoint (Body: `{"paths": ["..."]}`, Response: `{"ok": true, "ratings": {...}, "changed": N}`). Neue `IndexCache.patch_items()` Methode patcht einzelne Items im In-Memory-Cache (`dataclasses.replace()` auf frozen MediaItem). JS `refreshFolderRatings()` feuert asynchron nach `showPlaylist()`, patcht `allItems` + `playlistItems`, re-rendert via `applyFilter()` nur bei Änderungen. Dedup via `_ratingRefreshPath`. Audio-spezifisch (Video hat kein POPM); JS-Funktion in beiden UIs (guarded durch `RATING_WRITE_ENABLED`). 14 neue Tests: IndexCache.patch_items (8), Server-Endpoint (4), Feature-Parity (3, TestLazyRatingRefreshParity). `architecture.md` aktualisiert.
- **Auto-Resume deaktiviert (Audio)** (2026-04-09) — Neues Feature-Flag `enable_auto_resume` in `render_media_page()` / `render_player_js()` (Default `True`). JS-Variable `AUTO_RESUME_ENABLED` steuert, ob `loadAndSeekProgress()` beim Track-Wechsel aufgerufen wird. Audio-Server: `enable_auto_resume=False` — Songs starten immer von vorn, kein „Fortfahren bei"-Toast. Video-Server: Default `True` — Serien/Filme setzen an letzter Position fort. Progress wird unabhängig davon **immer** gespeichert.
- **Bewertungs-Sterne im Edit-Modal** (2026-04-09) — Das Metadaten-Bearbeitungs-Modal zeigt jetzt klickbare Rating-Sterne (`#edit-modal-rating`, `.edit-modal-rating-star`), vorausgefüllt mit dem aktuellen Track-Rating. Sichtbar nur wenn `RATING_WRITE_ENABLED`. Toggle: erneuter Klick auf gleichen Stern setzt auf 0 zurück. `submitEditModal()` speichert Metadata + Rating **parallel** via `Promise.all` (Metadata → `METADATA_EDIT_PATH`, Rating → `RATING_API_PATH`). Bei Erfolg: lokaler State, Player-Rating, gewichtete Shuffle-Queue aktualisiert. Neue JS-Funktionen: `renderEditModalRating()`, `_editModalRating`, `_initEditModalRatingEvents()`. CSS: `.edit-modal-rating`, `.edit-modal-rating-star.active/.hover`. 4 neue Tests.
- **Queue-Panel: Clip-Path-Animation** (2026-04-09) — Queue-Panel öffnet/schließt jetzt mit `clip-path: inset(100% 0 0 0)` → `inset(0)` statt `transform: translateY(100%)`. Effekt: Panel wächst visuell **von der Player-Bar-Oberkante nach oben** statt durchs Player-Overlay hindurchzugleiten.
- **Queue-Panel: Höhe passt sich Inhalt an** (2026-04-09) — `.queue-panel` nutzt `max-height` statt `height`, `.queue-body` nutzt `flex: 1 1 auto` statt `flex: 1 1 0`. Bei wenigen Titeln schrumpft das Panel auf die Inhaltshöhe (kein grauer Leerraum). Bei vielen Titeln begrenzt `max-height` und queue-body scrollt.
- **Queue-Panel: Drag-to-Resize + Display-Fix** (2026-04-09) — Drag-Handle am oberen Rand des Queue-Panels ermöglicht Höhenänderung per Mouse+Touch. Persistenz in `localStorage`. `overflow: hidden` auf `.queue-panel` behebt das Anzeigeproblem (Flex-Kinder respektieren `max-height`). Min-Höhe 220px. Neue CSS: `.queue-drag-handle`, `.queue-drag-handle-bar`, `.queue-panel.dragging`. Neue JS-Variablen: `_queueUserHeight`, `_QUEUE_HEIGHT_KEY`, `_QUEUE_MIN_H`. 3 neue Tests.
- **Warteschlange (Queue)** (2026-04-02) — Spotify-ähnliche Warteschlange: Queue-Button im Player-Bar (`#btn-queue`, `SVG_QUEUE`) mit Badge für Anzahl der Items. Queue-Panel als Bottom-Drawer (`.queue-panel`) mit Leeren-Button, Schließen-Button und Drag-and-Drop-Reorder. Track-Queue-Button (`.track-queue-btn`) pro Track in der Track-Liste — Toggle zum Hinzufügen/Entfernen. Playback-Integration: `dequeueNext()` hat Vorrang vor Shuffle und sequenziellem Modus bei `player ended` und `bgAudio ended`. Rein client-seitig (kein Backend-Endpoint, keine Persistenz). Neue JS-Variablen: `IC_QUEUE`, `IC_REMOVE`, `_userQueue`, `_queueOpen`, `_queueDndCleanup`. Listener-Lifecycle: `initQueueDragDrop()` / `destroyQueueDragDrop()` nach Regel 14. Swipe-Exclusion für Queue-Panel. Duplikat-Prüfung per `relative_path`. CSS: `.queue-panel`, `.queue-item`, `.queue-badge`, `.track-queue-btn`, `.queue-active`. 3 neue Feature-Parity-Tests (`TestQueueParity`). Funktioniert identisch für Audio und Video.
- **Rating-Schwellenwert (Min-Rating)** (2026-04-02) — Neue Konfigurationsoption `HOMETOOLS_MIN_RATING` (Env-Var, Default `0`, Bereich 0–5). Bewertete Tracks mit Rating ≤ Schwellenwert werden aus der Track-Liste ausgeblendet; unbewertete Tracks bleiben immer sichtbar. `get_min_rating()` in `config.py`, als `min_rating` Parameter durch `render_media_page()` → `render_player_js()` durchgereicht, JS-Variable `MIN_RATING_THRESHOLD`. Filterung in `applyFilter()` vor allen Quick-Filtern. Beide Server (Audio + Video) lesen aus Config. `.env.example` aktualisiert. 8 neue Tests: Config (4), JS-Injection (3), Feature-Parity (1).
- **Playlist-Redesign Sprint 5: Erweiterte Sync-Strategien + Insert-Position** (2026-04-02) — Drei Verbesserungen am Playlist-System: 1) **Optimistic UI** — Alle JS-Mutationsfunktionen (`deleteUserPlaylist`, `addToPlaylist`, `reorderPlaylistItem`, `movePlaylistItem`) wenden Änderungen sofort lokal an und revertieren per `_snapshotPlaylists()` / `_restorePlaylists()` bei Server-Fehler. Toast „Fehler … rückgängig" bei Rollback. 2) **Changelog-Retention** — `_rotate_changelog()` trimmt JSONL auf max 1000 Zeilen (`_MAX_CHANGELOG_LINES`). Wird nach jedem `_append_changelog()` aufgerufen. Atomare Schreibvorgänge. 3) **Konfigurierbares Polling-Intervall** — `HOMETOOLS_PLAYLIST_SYNC_INTERVAL` Env-Var (Default 30s, Minimum 5s). Wird als `playlist_sync_interval_ms` durch `render_player_js()` → `render_media_page()` injiziert statt hardcodiert. 4) **Insert-Position Quick Win** — `HOMETOOLS_PLAYLIST_INSERT_POSITION` Env-Var (`top`/`bottom`, Default `bottom`). `add_item()` akzeptiert neuen `insert_position` Parameter. Beide Server lesen aus Config. Diskussionspunkt aufgelöst. Neue Config-Funktionen: `get_playlist_insert_position()`, `get_playlist_sync_interval()`. `.env.example` aktualisiert. 17 neue Tests: Insert-Position (4), Changelog-Rotation (3), Config (6), Sync-Interval-Injection (2), Optimistic-UI (2). 4 neue Feature-Parity-Tests.
- **Playlist-Redesign Sprint 4: Cross-Device Playlist Sync** (2026-04-02) — Storage-Format v2: `{"revision": N, "playlists": [...]}` Envelope mit globalem Revisions-Counter. Jede Mutation (create/delete/rename/add/remove/move/reorder) inkrementiert `revision` und setzt `updated_at` auf der betroffenen Playlist. Legacy v1 (nacktes Array) wird transparent gelesen und beim ersten Schreiben migriert. Neues Changelog: `<cache_dir>/playlists/changelog_<server>.jsonl` (append-only JSONL, je Mutation eine Zeile). Neuer API-Endpoint `GET /api/<media>/playlists/version` (leichtgewichtig, nur `{"revision": N}`). `GET /api/<media>/playlists` enthält jetzt zusätzlich `revision` im Response. JS-Polling: `_startPlaylistSync()` startet 30s-Intervall, ruft `PLAYLISTS_VERSION_PATH` auf — bei höherer Server-Revision wird `loadUserPlaylists()` aufgerufen und Root-View aktualisiert. Pausiert bei `document.hidden` (Visibility API), startet bei Tab-Rückkehr mit sofortigem Check. Neue JS-Variable `PLAYLISTS_VERSION_PATH`. 30 neue Tests: Revision-Tracking (10), updated_at (5), Changelog (3), Backward-Compatibility (3), Version-Endpoint (6), Feature-Parity (3). Grundlage für optimistic UI und erweiterte Merge-Strategien (Sprint 5).
- **Playlist-Redesign Sprint 3: Server-seitige Ordner-Reihenfolge** (2026-04-02) — Neues Core-Modul `streaming/core/custom_order.py` persistiert benutzerdefinierte Item-Reihenfolgen (Ordner + Favoriten) server-seitig unter `<cache_dir>/custom_order/<server>/<md5_hash>.json`. Thread-sicher, atomare Schreibvorgänge. API-Endpoints `GET/PUT/DELETE /api/<media>/folder-order` in beiden Servern (Audio + Video). JS Dual-Source-Strategie: `_saveFolderOrder()` / `_saveFavoritesOrder()` schreiben in `localStorage` (sofort) + Server (fire-and-forget). `_loadFolderOrderAsync()` / `_loadFavoritesOrderAsync()` fetchen vom Server und re-sortieren die Ansicht bei Abweichung. `localStorage` als Offline-Fallback. Neue JS-Variable `FOLDER_ORDER_API_PATH`. 25 neue Tests (`test_custom_order.py`): 13 Unit-Tests, 8 Endpoint-Integration-Tests, 4 Feature-Parity-Tests. Grundlage für Cross-Device Playlist Sync (Sprint 4).
- **DnD Event-Listener-Lifecycle + Folder/Favorites Reorder** (2026-04-02) — 1) DnD-Listener-Lifecycle: `initPlaylistDragDrop()` speichert alle Event-Handler als named Functions in `_dndCleanup`. `destroyPlaylistDragDrop()` entfernt sie via `removeEventListener`. Wird aufgerufen in `showFolderView()`, `showPlaylist()`, und am Anfang von `initPlaylistDragDrop()`. Verhindert stale Listener nach View-Wechsel. Copilot-Instructions um Regel 14 (Event-Listener-Lifecycle) erweitert. 2) Filesystem-Ordner Reorder: `_currentPlaylistId = '__folder__'` in `showPlaylist()` aktiviert DnD für Ordner. Benutzerdefinierte Reihenfolge in `localStorage` (`ht-folder-order-<path>`). `_sortByFolderOrder()` / `_saveFolderOrder()` / `_loadFolderOrder()` Helpers. `reorderPlaylistItem()` erkennt `'__folder__'` und führt client-seitige Verschiebung durch. 3) Favoriten-Reorder: `_currentPlaylistId = '__favorites__'` aktiviert DnD. `ht-favorites-order` in `localStorage`. `_sortFavoritesByOrder()` / `_saveFavoritesOrder()` Helpers. 4) Stale-Context-Fix: `showPlaylist()` und `showFolderView()` setzen `_currentPlaylistId` explizit zurück, verhindert 404-Fehler bei DnD mit alter Playlist-ID.
- **Playlist-UX-Verbesserungen** (2026-04-02) — 1) Sort-Default „Liste" (custom) ist jetzt die erste Option im Dropdown und damit der Standard. 2) Automatische Favoriten-Playlist: Virtuelle `__favorites__`-Karte auf der Root-Startseite (vor User-Playlists), wird nur angezeigt wenn Favoriten vorhanden; Klick öffnet Browse-Ansicht, Play-Button spielt ab. 3) DnD-Insertion-Line-Fix: `updateDropTarget()` behält den Drop-Indicator bei wenn über dem eigenen Item gehovt wird (statt zurückzusetzen), verhindert visuelles Springen. 4) DnD jetzt auch im Listenmodus verfügbar (vorher nur filenames). 5) Click-Distance-Guard: Globaler `wasDrag(e)`-Check (6px Threshold) auf Ordner-/Datei-/Playlist-Karten und Track-Items verhindert versehentliches Navigieren/Abspielen bei Maus-Drag. 6) DnD-Visuell-Rework: Gezogenes Item kollabiert auf `max-height: 0` (statt `opacity: 0.25`), vertauschte CSS-`box-shadow`-Richtungen korrigiert (`drag-over-above` → Linie oben, `drag-over-below` → Linie unten), Linie auf 3px verbreitert. Desktop-Drag startet erst nach 10px Mausbewegung (Threshold verhindert Kollaps bei einfachem Klick). Neuer Fallback: Cursor unterhalb aller sichtbaren Items → letztes Item als Drop-Target mit `_dropAbove = false` (ermöglicht Verschieben ans Ende).
- **Playlist-Redesign Sprint 1+2: Pseudo-Ordner + UI-Cleanup** (2026-04-02) — Fundamentale Konzeptänderung: Playlists erscheinen jetzt als Pseudo-Ordner-Karten auf der Root-Startseite (nach „Downloaded"), statt über einen Header-Pill und ein separates Library-Panel zugänglich zu sein. Entfernt: `#playlist-pill` (Header-Button), `#playlist-library` (Overlay-Panel), `.playlist-lib-*` CSS-Klassen, `openPlaylistLibrary()`, `closePlaylistLibrary()`, `renderPlaylistLibrary()` JS. Neu: `.playlist-folder-card` mit Play- und Delete-Buttons, `.playlist-new-card` ("+Neue Playlist…"-Karte mit `prompt()`-Dialog). View-Toggle-Tooltips zeigen jetzt den aktiven Modus an ("Listenansicht — Klick für Kachelansicht" etc.). DnD-Reorder nur noch im `filenames`-Modus + Playlist-Kontext (`viewMode === 'filenames' && _currentPlaylistId`). Neue Sort-Option `custom` ("Liste ⇅") — in Playlists behält sie die Server-Reihenfolge bei, in Filesystem-Ordnern sortiert sie nach Rating absteigend. Feature-Parity-Test aktualisiert.
- **Ghost-Playlist-Fix + X-Button** (2026-04-02) — Root Cause: `create_app()` verwendete `get_cache_dir()` (echten Cache) auch wenn `library_dir` ein Test-`tmp_path` war. Tests erstellten so 46 Ghost-Playlists im echten `.hometools-cache/playlists/`. Fix: Neuer optionaler `cache_dir`-Parameter in `create_app()` für Audio- und Video-Server. Feature-Parity-Tests übergeben jetzt `cache_dir=tmp_path`. Echte Cache-Playlists geleert. X-Button (`.playlist-folder-del`) auf jeder Playlist-Karte: immer sichtbar (`opacity: 0.6`, voll auf Hover), mit `confirm()`-Dialog vor Löschung. TODO nach Entwicklungsphase: `confirm()` durch Archivierungs-Dialog ersetzen.
- **Playlist-Reordering (Drag-and-Drop)** (2026-04-02) — Neue `reorder_item()` Funktion in `streaming/core/playlists.py` verschiebt Items auf einen Ziel-Index (pop + insert, clamped). Neuer `PUT /api/<media>/playlists/items` Endpoint in beiden Servern (`{playlist_id, relative_path, to_index}`). UI: Desktop = Drag-and-Drop (mousedown → mousemove → mouseup), Mobile = Long-Touch 500ms aktiviert Drag-Modus. Ghost-Element (`.playlist-drag-ghost`) mit Thumbnail + Titel folgt dem Cursor/Finger. Insertion-Line via `box-shadow` (`.drag-over-above` / `.drag-over-below`). Auto-Scroll am Listenrand. `initPlaylistDragDrop()` wird am Ende von `renderTracks()` aufgerufen wenn `inPlaylist && _currentPlaylistId && viewMode === 'filenames'`. `_applyPlaylistUpdate()` Shared-Helper für `movePlaylistItem`/`reorderPlaylistItem`. `currentIndex` wird per `relative_path`-Match nach Reorder neu berechnet. Alte Pfeil-Buttons (`.track-reorder-btn`) entfernt. PATCH-Endpoint (`move_item`) bleibt für Abwärtskompatibilität erhalten. 9 neue Unit-Tests (`TestPlaylistReorderToIndex`), 1 neuer Feature-Parity-Test (`PUT`). Haptic-Feedback (`navigator.vibrate`) bei Long-Touch-Aktivierung. Keine externen Libraries.
- **Wiedergabelisten (User Playlists)** (2026-04-01) — Neues Core-Modul `streaming/core/playlists.py` mit CRUD-Funktionen (`load_playlists`, `create_playlist`, `delete_playlist`, `rename_playlist`, `add_item`, `remove_item`). Storage unter `<cache_dir>/playlists/<server>.json` (Audio/Video getrennt). Thread-sicher via `threading.Lock`, atomare Schreibvorgänge. Max 50 Playlists, 500 Items/Playlist. API-Endpoints in beiden Servern: `GET/POST/DELETE /api/<media>/playlists`, `POST/DELETE /api/<media>/playlists/items`. Feature-Flag `enable_playlists=True` in `render_media_page()`. UI: Playlist-Pill im Header, Playlist-Library-Panel (Übersicht + Play/Delete), „Zur Playlist hinzufügen"-Modal mit Dropdown + Inline-Erstellen, `.track-playlist-btn` pro Track. `SVG_PLAYLIST`/`IC_PLAYLIST` Icon. Swipe-Exclusion für Playlist-Panels. 20 neue Unit-Tests (`test_playlists.py`), 5 Feature-Parity-Tests. Grundlage für Playlist-Reordering (Follow-Up).
- **Genre-Tags bei Musik nutzen** (2026-03-31) — `get_genre()` in `audio/metadata.py` liest Genre-Tags (ID3 TCON, MP4 ©gen, Vorbis genre/GENRE). Neues `genre: str`-Feld auf `MediaItem`. `build_audio_index()` liest Genre beim Index-Build. Genre-Filter-Chip (`#filter-genre`) in der Filter-Bar zykliert durch verfügbare Genres (alphabetisch sortiert → alle). AND-Logik mit bestehenden Filtern. `localStorage`-Persistenz (`ht-filter-genre`). Chip versteckt sich automatisch wenn keine Genre-Tags vorhanden. 8 neue Tests (3 metadata + 5 UI). Grundlage für „Ähnliche Titel"-Feature.
- **Audit-Log aus Cache-Verzeichnis herausgelöst** (2026-03-31) — `audit.jsonl` aus `.hometools-cache/audit/` nach `.hometools-audit/` (eigenes Verzeichnis) verschoben. Neue `get_audit_dir()` in `config.py` mit `HOMETOOLS_AUDIT_DIR` Env-Var. Alle `audit_log.py`-Funktionen akzeptieren jetzt `audit_dir` statt `cache_dir`. Automatische Migration: Server kopiert beim Start `<cache_dir>/audit/audit.jsonl` → `<audit_dir>/audit.jsonl` (einmalig, idempotent, nie überschreibend). `make clean` löscht jetzt den gesamten Cache ohne Audit-Ausnahme. `.env.example` und `.gitignore` aktualisiert. 6 neue Tests (4 Migration + 2 Config). Beide Server (Audio + Video) umgestellt.
- **Swipe-Gesten für mobile Navigation** (2026-03-31) — Touch-Swipe-Handler (`touchstart`/`touchend`) für Zurück-Navigation auf Mobilgeräten. Swipe rechts = zurück (`goBack()`) in Ordner- und Playlist-Ansicht. Track-Wechsel ausschließlich über Buttons. Schwellenwerte: 60px min-dist, 80px max-vert, 400ms max-time. Ausnahmen: Range-Inputs, Canvas, Modals, Lyrics-Panel, Offline-Library. Passive Event-Listener. Kein Feature-Flag (universell). 4 Tests in `test_streaming_player_ui.py`.
- **Offline-Downloads-Liste** (bereits implementiert) — `refreshOfflineLibrary()`, `renderOfflineDownloadList()` in `server_utils.py`, Offline-Modal mit Sortierung, IndexedDB-Integration.
- **Channel-Server: Playlist-basiert (Rewrite)** (2026-03-31) — HLS-Livestream-Architektur (`server.py` + `mixer.py`) durch einfachen **Playlist-basierten Server** (`server_playlist.py`) ersetzt. Der neue Server baut eine Tagesplaylist aus dem YAML-Schedule (`fill_series` + geplante Slots → interleaved `MediaItem`-Liste) und nutzt den bestehenden Video-Player mit Auto-Next (`player.addEventListener('ended', nextIndex())`). Kein ffmpeg-Hintergrundprozess, keine HLS-Segmente, keine Race Conditions. Standard-UI via `render_media_page()`, gleiche Endpoints wie Audio/Video (`/api/channel/items`, `/api/channel/progress`, etc.). Alter HLS-Code bleibt als `server.py` erhalten, wird nicht mehr als Default verwendet. 13 neue Tests (`test_channel_playlist.py`).
- **Channel-Mixer Rewrite: Concat Demuxer + Pre-Transcode** (2026-03-25) — Fundamentale Architekturänderung: Alte Multi-Prozess-Architektur (ein ffmpeg pro Video) durch **Concat Demuxer** ersetzt. Neues Modul `streaming/channel/transcode.py` mit `prepare_video()`, `prepare_testcard()`, `build_concat_file()`, `cleanup_prepared()`. Videos werden in `.hometools-cache/channel/tmp/` vorab auf H.264/AAC 1280×720 25fps konvertiert, dann über **einen** ffmpeg-Prozess (`-f concat -c copy → -f hls`) lückenlos gestreamt. Temporäre Dateien werden nach Wiedergabe gelöscht. Entfernte Workarounds: `_sync_segment_counter_from_disk()`, `_cleanup_stale_manifest()`, globale `_segment_counter`-Variable. Config: `get_channel_tmp_dir()`. 88 Tests (davon 15 neue für Transcode/Concat). **Veraltet — abgelöst durch Playlist-Server (2026-03-31).**
- **Fernsehsender (Channel-Server)** — Dritter Server auf Port 8012 (`serve-channel`), kontinuierlicher HLS-Livestream via ffmpeg; YAML-Programmplan (`channel_schedule.yaml`) mit Tages-/Wochentag-Slots; pünktlicher Serienstart mit automatischer Seek-Berechnung bei Late-Join; Filler-Content (Clips/Musik) für Lücken; Episode-State-Tracking (sequential/random); `ChannelMixer` Background-Thread; Browser-UI mit hls.js + EPG + „Jetzt läuft"; `HOMETOOLS_CHANNEL_PORT`, `HOMETOOLS_CHANNEL_SCHEDULE`, `HOMETOOLS_CHANNEL_FILLER_DIR`, `HOMETOOLS_CHANNEL_ENCODER` Env-Vars; 36 Tests
- **Schnellfilter-Chips (Track-Liste)** — Pill-Buttons „Bewertung" (zyklisch 1–5★ Minimum) und „Favoriten" (Toggle) in der Filter-Bar; AND-Logik mit Textsuche; `localStorage`-Persistenz; `updateFilterChips()` in `server_utils.py`; CSS-Klasse `.filter-chip` + `.active`.
- **Tools-Panel (UI-Einstellungen)** — „Tools"-Pill neben „Downloaded" im Header; Modal-Panel mit Toggle-Switches für: Inline-Ratings (Bewertungssterne direkt in der Track-Liste), Downloads (Buttons ein/ausblenden), Playlists (Buttons ein/ausblenden), Duplikate suchen; `ht-tools` localStorage-Persistenz; CSS-only Visibility via `body`-Klassen (`tool-inline-ratings`, `tool-hide-downloads`, `tool-hide-playlists`, `tool-show-duplicates`); Inline-Ratings nutzen `setInlineRating()` mit `POST /api/<media>/rating` und in-place DOM-Update; DnD-Exclusion für `.track-inline-rating-star`; Shared Core (`server_utils.py`), kein Server-Flag nötig.
- **Duplikat-Erkennung (Client-Side)** (2026-04-14) — Rein client-seitige Duplikat-Erkennung über die geladene `allItems`-Liste. JS-Port von `stem_identifier()` und `split_extreme()` aus `audio/sanitize.py`: `_normalizeStem()` normalisiert Dateinamen (feat./prod./vs., URLs, Bitrate-Tags, Emojis entfernen), `_dupeKey()` erzeugt stabilen Key (aggressives Splitting, Musik-Keywords entfernen, sortierte Parts). `_buildDuplicateMap()` gruppiert Items nach Key, `_dupePaths` Set für O(1)-Rendering-Lookup. UI: orangenes `.dupe-badge` ("Duplikat") am Track-Titel (via CSS `body.tool-show-duplicates`), Show-Link im Tools-Panel ("N Duplikat-Gruppen gefunden — anzeigen"), Duplikat-Panel (`.dupe-panel`) mit Gruppen-Liste (Thumbnail, Titel, Ordner, Click-to-Play). Cache invalidiert bei `allItems`-Replacement. `SVG_DUPLICATE` / `IC_DUPLICATE` Icon. Shared Core, kein Backend-Endpoint.
- **Dateien verschieben (File Mover)** (2026-04-15) — Tool zum Verschieben von Audio-Dateien in andere Ordner der Bibliothek. Backend: `POST /api/audio/move-file` (`{ path, target_folder }` → `shutil.move` + Audit-Log `file_move`), `GET /api/audio/folders` (Ordnerliste), Undo via `file_move`-AuditEntry im `/api/audio/audit/undo`-Handler. Frontend: `renderMoveWidget(t, idx)` generiert pro Track (1) aktuellen Ordner als farbiges Tag (`.move-current-folder`), (2) 2×2-Grid der letzten 4 Ordner als Quick-Pick-Buttons (`.move-quick-grid` + `.move-quick-btn`), (3) `<select>` Dropdown mit allen Ordnern (`.move-folder-select`). `moveFileToFolder()` sendet Move-Request, aktualisiert `allItems` in-place, invalidiert Caches. MRU-Ordner in `localStorage` (`ht-move-recent`). Tools-Toggle `fileMover` mit CSS-Klasse `body.tool-show-file-mover`. `SVG_MOVE` Icon. Exklusiv-Modus: blendet andere Track-Buttons aus wenn aktiv.
- **Duplikat-Löschung (Soft-Delete)** (2026-04-15) — Trash-Button (Mülleimer-Icon `SVG_TRASH`/`IC_TRASH`) pro Item im Duplikat-Panel. Nur Duplikate sind löschbar, kein allgemeiner Delete in der UI. Backend: `POST /api/audio/delete-file` und `POST /api/video/delete-file` (Feature-Parity), Body `{ "path": "..." }`, Soft-Delete via `attention_delete_files()` (verschiebt in `HOMETOOLS_DELETE_DIR`), Audit-Log `file_delete` via `log_file_delete()`. Frontend: `_deleteDuplicateFile(allIndex)` mit `confirm()`-Dialog, `allItems.splice()`, Cache-Invalidierung, Panel-Re-Render. CSS: `.dupe-trash-btn` (transparent, rot-auf-hover). `stopPropagation()` verhindert Click-to-Play des Parent.
- **Songtexte anzeigen (Lyrics-Panel)** — Bottom-Drawer `.lyrics-panel` im Audio-Player; lazy Fetch via `GET /api/audio/lyrics?path=`; `_lyricsCache` verhindert Mehrfachfetches; auto-Update beim Track-Wechsel wenn Panel offen; `SVG_LYRICS` / `IC_LYRICS`; `enable_lyrics=True` nur im Audio-Server; `LYRICS_ENABLED` / `LYRICS_API_PATH` JS-Variablen.
- **`make clean`** — löscht alle generierten Cache-Dateien (Thumbnails, Indexes, Progress, Issues, Logs, Shortcuts, `video_metadata_cache.json`, `thumbnail_failures.json`) unter `.hometools-cache/` und schont dabei `audit/` explizit. Python-basiert, Windows-kompatibel. Hinweis im `help`-Target und `get_cache_dir()`-Docstring.
- **Thumbnail-Größen je Ansichtsmodus** — Kleine Thumbs nur in Listenansicht (`viewMode === 'list'`); Grid- und Filenames-Modus verwenden große Thumbnails (`thumbnail_lg_url`). Player-Bar zeigt immer große Version.
- **Zuletzt gespielt — server-spezifisch** — Audio: `enable_recent=False` (kein UI-Abschnitt; Hörbücher steigen via Progress-API ein). Video: max. 3 Folgen, 14 Tage, 1 je Serie — konfigurierbar via `HOMETOOLS_RECENT_VIDEO_LIMIT`, `HOMETOOLS_RECENT_MAX_AGE_DAYS`, `HOMETOOLS_RECENT_MAX_PER_SERIES` (in `.env.example` dokumentiert).
- **Ansichtsumschalter (3 Modi)** — list → grid → filenames zyklisch; `filenames`-Modus ersetzt separate „Original"-Checkbox; in `architecture.md` dokumentiert.
- **Metadaten-Bearbeitung (Audio)** — `POST /api/audio/metadata/edit` Endpoint, `write_track_tags()` (MP3/M4A/FLAC/OGG), `enable_metadata_edit=True` in Audio-Server, Bleistift-Button (`.track-edit-btn`, `IC_EDIT`) pro Track in Liste, Edit-Modal (`.edit-modal-backdrop`, Titel/Interpret/Album), lokaler JS-State-Update ohne Server-Round-Trip, Audit-Integration via `log_tag_write()`, 7 neue Tests
- **Zuletzt gespielt / Continue Watching** — `get_recent_progress()` in `progress.py`, `GET /api/<media>/recent` (Audio + Video), „Zuletzt gespielt"-Scroll-Sektion auf Startseite mit Fortschrittsbalken + direkter Seek-Wiederaufnahme, `RECENT_API_PATH` JS-Variable
- **Hörbuch-Ordner-Erkennung** — `get_audiobook_dirs()` + `is_audiobook_folder()` in `config.py`, `HOMETOOLS_AUDIOBOOK_DIRS` Env-Var, `AUDIOBOOK_DIRS` JS-Array, blaue Einfärbung (`.audiobook-folder`) im Ordner-Grid
- **M4A-Rating Race-Condition-Fix** (2026-04-10) — `refreshCatalog()` JS-Funktion prüft jetzt `data.refreshing` und pollt via `scheduleBackgroundRefresh()` bis der Index-Rebuild fertig ist, statt veraltete cached Daten als final anzuzeigen. `audio_set_rating` Endpoint patcht den In-Memory-Cache sofort per `patch_items()` vor `invalidate()`, sodass die API das korrekte Rating auch vor Background-Rebuild liefert. `_SNAPSHOT_VERSION` auf v4 gebumpt — verwaiste Snapshots mit falschen M4A-Ratings (0.0) werden beim Laden verworfen.
- Audit-Log & Change-Log — `streaming/core/audit_log.py`, JSONL append-only, `GET/POST /api/<media>/audit`, `POST /api/<media>/audit/undo`, Control Panel `/audit`, Undo-Toast in App mit `entry_id`, Bewertungsverlauf pro Datei über `?path_filter=`
- Songwertung-Schreiben (Audio-only) — `POST /api/audio/rating` Endpoint, `enable_rating_write=True` in Audio-Server, 5 klickbare Sterne im Player (`SVG_STAR_EMPTY`, `IC_STAR_FILLED`/`IC_STAR_EMPTY`), Hover-Preview, POPM-Write via `set_popm_rating()`, Index-Invalidierung, Weighted-Shuffle-Queue-Rebuild nach Rating-Änderung
- Shuffle-Modus (Audio-Server) — `enable_shuffle=True` in `render_media_page()`, Shuffle-Button im Player-Bar (Classic + Waveform), Fisher-Yates + gewichteter Shuffle nach POPM-Rating, Long-Press für Weighted-Modus, localStorage-Persistenz, Offline-kompatibel (client-seitige Queue), implementiert in Core (`server_utils.py`)
- Header-Navigation: Emoji-Symbol als Startseiten-Button (`logo-home-btn`), Titel als reiner Text (`logo-title`) — kein Link mehr
- Favoriten-Badge SVG (Fix 1: `IC_STAR`/`SVG_STAR` statt `&#9733;` Unicode-Stern; `.fav-badge` CSS auf SVG umgestellt)
- Verwaiste CSS-Zeilen entfernt (Fix 2: orphaned `padding`/`}` nach `.track-dl-btn svg`)
- Rating-System POPM (`get_popm_rating()`, ID3-POPM → 0–5 Sterne, `MediaItem.rating`, `.rating-bar` UI)
- Server-Logging in Datei (`get_log_dir()`, `RotatingFileHandler`, `<cache_dir>/logs/hometools.log`)
- Folder-Favorites (Namens-Konvention `#`-Prefix, SVG-Badge, alphabetisch zuerst sortiert)
- Phase 2: PWA Shortcuts — Deep Linking (`?id=`), Shortcuts API + JSON-Storage, Manifest-Generator, Pin-Button pro Item, Share-Sheet/Clipboard
- Phase 1: Offline-Download Feature (PWA) — Service Worker, IndexedDB, Offline-Playback
- FastStart-Erkennung für MP4-Dateien (`has_faststart()`, Auto-Remux)
- SVG-Icons statt Unicode/Emoji (alle Buttons, iOS-kompatibel)
- Shadow-Cache ins Repository (`.hometools-cache/`, `HOMETOOLS_CACHE_DIR`)
- **Rating-Filter: Ausgeblendet-Chip + Graying-Verbesserung** (2026-05-15) — `filter-hidden`-Chip zeigt jetzt immer `(N/M)`-Format (Anzahl ausgeblendet / Gesamtanzahl), auch wenn N=0, damit der Button nicht in der Breite springt. CSS `min-width: 8.5rem` + `justify-content: flex-start` auf `#filter-hidden` für stabiles Layout. Opacity auf 0.4 reduziert, `filter: saturate(0.2)` auf `.track-item--hidden-shown` für deutlich sichtbares Grau. Explizite Textfarben (`.track-title-text`, `.track-artist`) in ausgeblendeten Items. „ausgeblendet"-Suffix im `trackCount`-Text entfernt (Info ist jetzt im Chip-Button); `track-count` bleibt damit in stabiler Breite.
- **Audit-Log Bugfix + Service-Worker-Routing-Fix** (2026-05-16) — Zwei Bugs behoben: (1) **SW-Routing-Bug:** Der Service-Worker-Streaming-Check (`url.pathname.includes('/audio/')`) fing fälschlicherweise alle `/api/audio/...`-Pfade ab (statt nur die echten Streaming-Pfade `/audio/stream`, `/video/stream`). Der Streaming-Handler hat keinen Error-Fallback — Netzfehler des Servers propagierten direkt und triggerten `.catch` im Audit-JS → Toast „Fehler beim Laden der Einträge". Fix: API-Check (`startsWith('/api/')`) vor den Streaming-Check verschoben; Streaming-Check nutzt jetzt `startsWith('/audio/')` statt `includes`. SW-Cache-Version auf `hometools-v8`. (2) **Audit-JS-Fehlerbehandlung:** Bei non-OK HTTP-Antworten (z.B. 503 vom API-Fallback) blieb vorher ewig „Lade…" in der Tabelle ohne Rückmeldung. Fix: non-OK-Responses werden jetzt zu einem rejected Promise konvertiert → `.catch` zeigt Toast + persistente Fehlermeldung in der Tabelle. (3) **Fehlender E2E-Test hinzugefügt:** `test_audio_rating_write_creates_audit_entry` prüft den vollständigen Pfad: `POST /api/audio/rating` → Audit-Entry in `GET /api/audio/audit` sichtbar. (4) **SW-Handler-Reihenfolge-Test hinzugefügt:** `test_service_worker_api_check_before_streaming_check` stellt via String-Positions-Prüfung sicher, dass der API-Check im generierten SW-Code immer vor dem Streaming-Check steht.
- **Audit-Log Exception-Safety + Test-Isolation** (2026-05-16) — (1) **Exception-Safety:** `_audit_path(audit_dir)` wurde in `load_entries`, `get_entry` und `append_entry` außerhalb von `try/except` aufgerufen — ein `OSError` beim `mkdir` (Berechtigungsfehler, Read-only-FS) führte zu HTTP 500, das den „Fehler beim Laden der Einträge"-Toast auslöste. Fix: alle drei Funktionen fangen `_audit_path`-Fehler mit `try/except`, loggen und geben `[]` / `None` / silent return zurück. (2) **`audit_dir`-Parameter in `create_app()`:** Audio- und Video-Server akzeptieren jetzt einen optionalen `audit_dir: Path | None = None`-Parameter — fällt auf `get_audit_dir()` zurück wenn `None`. Tests übergeben `audit_dir=tmp_path / "audit"` direkt statt mit `monkeypatch.setenv`. (3) **Neuer Test:** `test_audio_audit_endpoint_inaccessible_dir_returns_empty` verifiziert, dass der Audit-Endpoint bei nicht erstellbarem Verzeichnis `200 + {"items": []}` zurückgibt (nicht 500). (4) **Retry-Button im Fehlerfall:** `loadEntries`-Fehler zeigt HTTP-Statuscode im Toast und fügt einen „Erneut versuchen"-Knopf in die Tabelle ein.


- Wiedergabe-Fortschritt speichern (`progress.py`, Resume-Toast)
- Recently Added Sortierung (`mtime`-Feld, „Neueste ⇅")
- On-the-fly Remux/Transcode (`remux.py`, FLV/AVI/MKV → frag-MP4)
- Serien-Episoden-Ordnung (`parse_season_episode()`, S##E## / ##x##)
- YAML-Overrides (`hometools_overrides.yaml`, Anzeigenamen/Staffel/Episode)
- Action Hints (strukturierte CLI-Empfehlungen pro Task-Kategorie)
- Noise-Unterdrückung (quellspezifische Schwellen, `_NOISE_RULES`)
- Root-Cause-Deduplizierung (`_ROOT_CAUSE_PATTERNS`)
- CLI-Dashboard (`stream-dashboard`, Box-Drawing, `--json`)
- Issues/Tasks aus Browser-UI entfernt (nur API + CLI)
- Cache-First API (Server sofort nutzbar während Index-Rebuild)
- Verwaiste `.tmp_*`-Testverzeichnisse aufgeräumt
