{
  "name": "hometools-streaming-webui",
  "private": true,
  "version": "0.0.1",
  "description": "Vite/TypeScript build for the hometools streaming player UI. Builds static JS/CSS consumed by the FastAPI server_utils HTML skeleton. See README.md for migration status.",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}

